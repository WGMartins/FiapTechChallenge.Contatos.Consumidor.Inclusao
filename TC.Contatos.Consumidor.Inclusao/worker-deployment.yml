apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumidor-inclusao
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumidor-inclusao
  template:
    metadata:
      labels:
        app: consumidor-inclusao
    spec:
      containers:
      - name: consumidor-inclusao
        image: fiaptechchallenge.azurecr.io/contatos.consumidor.inclusao.worker
        envFrom:
          - configMapRef:
             name: configmap-worker